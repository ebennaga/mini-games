import { Box, Typography } from '@mui/material';
import React from 'react';

const MissionPage = () => {
    return (
        <Box>
            <Typography>Mission Page</Typography>
        </Box>
    );
};

export default MissionPage;
