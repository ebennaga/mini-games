import { Skeleton } from '@mui/material';
import React from 'react';

const InfoCardSkeleton = () => {
    return <Skeleton sx={{ height: '180px', marginTop: '-50px' }} />;
};

export default InfoCardSkeleton;
